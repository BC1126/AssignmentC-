@model PaymentVM

@section head {
    <link href="/css/ticket.css" rel="stylesheet" />
}

@{
    int totalMinutes = Model.Booking.ShowTime.Movie.DurationMinutes;
    int hours = totalMinutes / 60;
    int minutes = totalMinutes % 60;
}

<partial name="_Receipt" model="Model"/>
